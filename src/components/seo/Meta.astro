---
import type { HTMLAttributes } from "astro/types"

interface Props extends HTMLAttributes<"meta"> {
	property?: string
}
const { property, ...attrs } = Astro.props
---

<meta {property} {...attrs} />
